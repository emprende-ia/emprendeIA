# Patrones anclados a la raíz con `/` para no chocar con paths del código
# (e.g. `supabase/` sin `/` líder excluiría también `src/lib/supabase/`).

# Cosas legacy de Firebase que no van a Vercel
/firebase.json
/firestore.rules
/firestore.indexes.json
/storage.rules
/database.rules.json
/remoteconfig.template.json
/apphosting.yaml
/dataconnect/
/functions/
/.firebaserc
/firebase-debug.log

# Migraciones SQL — se aplican a Supabase, no se sirven (raíz solamente)
/supabase/

# Archivos del proyecto que no van a producción
/estructura.txt
/studio.json
/.agents/
/.claude/
/.vscode/
